# Copy to .env.deploy and fill in values (do not commit .env.deploy).
# EC2_HOST / EC2_PEM are only for: ./scripts/deploy-aws.sh local --ssh
AWS_REGION=
AWS_ACCOUNT_ID=
ECR_REPOSITORY=
SSM_PREFIX=
EC2_APP_DIR=
EC2_HOST=
EC2_PEM=